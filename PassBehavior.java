/**
 * PassBehavior implements OffenseBehavior and dictates what happens when a player passes.
 * @author bryson Carroll
 *
 */
public class PassBehavior implements OffenceBehavior {
	public String play()
	{
		return "Passes to a forward";
	}
}
